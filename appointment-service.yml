eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      default-zone: http://localhost:8761/eureka
  instance:
    instance-id: appointmentapi.internal.wittydune-ac317a4f.eastus.azurecontainerapps.io:${random.value}}

